# Блок-схема алгоритму клієнтської фільтрації

Ось код для PlantUML (використовуйте на [PlantUML.com](https://www.plantuml.com/plantuml/)):

```plantuml
@startuml
title Рис. 2.2.1 – Блок-схема алгоритму клієнтської фільтрації та відображення каталогу

start

:Користувач відкриває сторінку каталогу;
:Завантаження повного списку тайтлів з API;
:Ініціалізація стану фільтрів (Genre, Status, Type);

partition "Цикл очікування дій" {
    repeat
        :Очікування вибору фільтра користувачем;
        
        if (Вибрано новий фільтр?) then (так)
            :Оновлення об'єкта стану filters;
        else (ні)
            if (Введено текст у пошук?) then (так)
                :Оновлення query стану;
            endif
        endif

        :Виклик функції filteringLogic();

        partition "Процес фільтрації (Client-side)" {
            :Копіювання оригінального масиву даних;
            
            if (Фільтр 'Тип' активний?) then (так)
                :Відсіювання за типом (Манґа/Манхва);
            endif

            if (Фільтр 'Статус' активний?) then (так)
                :Відсіювання за статусом (В процесі/Завершено);
            endif

            if (Фільтр 'Жанри' не порожній?) then (так)
                :Перевірка наявності ВСІХ обраних жанрів у тайтлі;
            endif

            if (Пошуковий запит не порожній?) then (так)
                :Пошук за регулярним виразом у Title та AltTitle;
            endif
        }

        :Оновлення стану filteredData;
        :Тригер рендерингу React-компонентів;
        :Відображення оновленої сітки карток;

    repeat while (Користувач продовжує роботу?) is (так)
}

stop
@enduml
```

### Як цим користуватися:
1. Скопіюйте код між `@startuml` та `@enduml`.
2. Перейдіть на сайт [PlantUML Online Server](https://www.plantuml.com/plantuml/).
3. Вставте код у текстове поле.
4. Ви отримаєте професійну блок-схему для вашої документації.
